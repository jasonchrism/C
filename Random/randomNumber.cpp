#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <string.h>

int main(){

    // srand(time(0));
    // int num = (rand() % 100 + 1) * 7000;
    // printf("%d", num);

    // char name[100];
    // char temp[100];
    // scanf("%s", name); getchar();
    // strncpy(temp, name, 2);
    // temp[2] = 0;
    // printf("%s", temp);

}